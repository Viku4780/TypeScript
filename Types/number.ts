// number both integer and float are comes in number category

let userId: number = 334455;

// boolean
let isLoggedIn = false;
// type inference if we want to make the variable obvious i mean for a primitives type variable there is no need to writ type safety typescript understand at the time declaration and iunteranlly it understand what type of vlue should this variable hold


// any

let hero: string;

function getHero(){
    return "thor";
}

hero = getHero();





export {}